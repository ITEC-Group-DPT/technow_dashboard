import { height } from "@mui/system";

const styles = {
    card: {
        filter: "drop - shadow(0px 1px 1px rgba(0, 0, 0, 0.15))",
        my: 2,
        borderRadius: '10px'
    }
}

export default styles;